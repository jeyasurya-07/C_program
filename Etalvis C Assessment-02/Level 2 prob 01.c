// check if num == 50 without if else,,, by storing relational operation in var return 1/0
#include <stdio.h>

int main()
{
    int x,y=50;
    printf("Enter Nmber:");
    scanf("%d",&x);
    y=x==y;

    printf("Result = %d",y);
}